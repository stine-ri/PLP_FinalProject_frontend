import React from 'react';
import {TeacherAttendance} from '../../Components/Teachers/AttendanceForm';

const Attendance: React.FC = () => (
  <div>
    <TeacherAttendance />
  </div>
);

export default Attendance;